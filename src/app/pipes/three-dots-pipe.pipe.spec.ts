import { ThreeDotsPipePipe } from './three-dots-pipe.pipe';

describe('ThreeDotsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ThreeDotsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
